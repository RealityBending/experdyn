% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/resample_timeseries.R
\name{.safe_spline}
\alias{.safe_spline}
\title{Safe Natural Cubic Spline Interpolation}
\usage{
.safe_spline(x, y, xout, method = "natural", min_pts = 4)
}
\arguments{
\item{x}{Numeric vector of known \emph{x} positions (e.g., time in seconds).}

\item{y}{Numeric vector of known \emph{y} values (possibly containing \code{NA}s).}

\item{xout}{Numeric vector of output positions at which to evaluate the spline.}

\item{method}{Character string passed to \code{\link[stats:splinefun]{stats::spline()}}. Default \code{"natural"}.}

\item{min_pts}{Minimum number of non-\code{NA} observations required; if fewer
are available, a vector of \code{NA_real_} of length \code{length(xout)} is returned.}
}
\value{
Numeric vector of interpolated values, the same length as \code{xout}.
Positions outside \verb{[min(x_valid), max(x_valid)]} are returned as \code{NA}.
}
\description{
A thin wrapper around \code{\link[stats:splinefun]{stats::spline()}} that gracefully handles \code{NA} values
and series that are too short to be interpolated. Output is clamped to the
range of valid observations — no extrapolation beyond the data.
}
\keyword{internal}
